import React from 'react'
import type { JSX } from 'react/jsx-runtime'

import { useLocation } from 'react-router-dom'


    
// Component

        function SectionLabel({
            label
        }: {
            label: string;
        }) {
            return (
                <div data-v-716c1e5e={""} className={"section-label"}>
                    {label}
                </div>
            );
        }
    

export default SectionLabel
